name: common
description: Common module for shared components and utilities
version: 1.0.0

environment:
  sdk: ">=3.1.3 <4.0.0"
  flutter: ">=3.1.3"

dependencies:
  flutter:
    sdk: flutter
  
  # Image loading and caching
  cached_network_image: ^3.3.1
  
  # Image picker
  image_picker: ^1.0.4
  
  # Permissions
  permission_handler: ^11.0.1
  
  # Utilities
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^5.1.0

flutter: